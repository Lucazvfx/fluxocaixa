# PDF Merge Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir upload de múltiplos PDFs simultaneamente, somando os contadores de animais, nos dois painéis da interface.

**Architecture:** Novo endpoint `/api/ler-pdfs` no backend Python que aceita N arquivos, processa cada um com os parsers existentes (IDARON/INDEA), soma `valores[0..9]`, e retorna o mesmo formato JSON que o endpoint singular. O frontend envia todos os arquivos de uma vez via `FormData` com campo `pdf` repetido.

**Tech Stack:** Flask (Python), pdfplumber, JavaScript vanilla (fetch API)

---

## Arquivos Modificados

| Arquivo | O que muda |
|---|---|
| `app.py` | Adiciona endpoint `POST /api/ler-pdfs` após linha 397 |
| `templates/index.html` | Inputs com `multiple`, funções `lerPDF` e `lerPDFInserir` atualizadas |

---

### Task 1: Backend — endpoint `/api/ler-pdfs`

**Files:**
- Modify: `app.py` — inserir novo endpoint logo após o bloco do `api_ler_pdf` (após linha 397, antes da linha `@app.route('/api/modelo-info'`)

- [ ] **Step 1: Adicionar o endpoint em `app.py`**

Inserir imediatamente após a linha `        pass` do bloco `finally` do `api_ler_pdf` (linha ~397), antes de `@app.route('/api/modelo-info'`:

```python
@app.route('/api/ler-pdfs', methods=['POST'])
@login_required
def api_ler_pdfs():
    arquivos = request.files.getlist('pdf')
    if not arquivos:
        return jsonify({'erro': 'Nenhum arquivo enviado'}), 400

    valores_acc = [0] * 10
    meta = {'fazenda': '', 'municipio': '', 'proprietario': '', 'cpf': '', 'ie': '', 'data_saldo': ''}
    processados = 0
    erros = []

    for f in arquivos:
        if not f.filename.lower().endswith('.pdf'):
            erros.append(f'{f.filename}: não é PDF')
            continue
        with tempfile.NamedTemporaryFile(suffix='.pdf', delete=False) as tmp:
            tmp_path = tmp.name
            f.save(tmp_path)
        try:
            text = extrair_texto_pdf(tmp_path)
            orig = detectar_origem(text)
            if orig == 'IDARON':
                dados = parsear_idaron(text, pdf_path=tmp_path)
            elif orig == 'INDEA':
                dados = parsear_indea(text)
            else:
                dados = parsear_idaron(text, pdf_path=tmp_path)
                if dados['total'] == 0:
                    dados = parsear_indea(text)
            for i, v in enumerate(dados['valores']):
                valores_acc[i] += v
            for campo in ('fazenda', 'municipio', 'proprietario', 'cpf', 'ie', 'data_saldo'):
                if not meta[campo] and dados.get(campo):
                    meta[campo] = dados[campo]
            processados += 1
        except Exception as e:
            erros.append(f'{f.filename}: {e}')
        finally:
            try:
                os.unlink(tmp_path)
            except OSError:
                pass

    if processados == 0:
        return jsonify({'erro': 'Nenhum PDF válido processado', 'erros': erros}), 400

    return jsonify({
        **meta,
        'valores': valores_acc,
        'total': sum(valores_acc),
        'pdfs_processados': processados,
        'erros': erros,
    })
```

- [ ] **Step 2: Verificar que o servidor sobe sem erro**

```bash
cd c:/Users/Lucas/Downloads/boviml_python/boviml
python -c "import app; print('OK')"
```

Esperado: `OK` sem traceback.

- [ ] **Step 3: Testar o endpoint manualmente**

Com o servidor rodando (`python app.py`), abrir o browser no painel "Ler PDF", selecionar dois PDFs. O resultado deve mostrar a soma dos animais dos dois arquivos e `pdfs_processados: 2` no JSON retornado (visível no DevTools → Network).

- [ ] **Step 4: Commit**

```bash
git add app.py
git commit -m "feat: endpoint /api/ler-pdfs que soma múltiplos PDFs"
```

---

### Task 2: Frontend — painel "Ler PDF"

**Files:**
- Modify: `templates/index.html`
  - Linha ~446: input do upzone — adicionar `multiple`, mudar onchange
  - Linha ~1113-1124: função `lerPDF`
  - Linha ~1148: reconstrução do upzone após carregamento (`mostrarPDFPreview`)

- [ ] **Step 1: Atualizar o `<input>` do upzone (linha ~446)**

Localizar:
```html
<input type="file" id="pdf-inp" accept=".pdf" style="display:none" onchange="lerPDF(this.files[0])">
```

Substituir por:
```html
<input type="file" id="pdf-inp" accept=".pdf" multiple style="display:none" onchange="lerPDF(this.files)">
```

- [ ] **Step 2: Atualizar o texto da upzone (linha ~443)**

Localizar:
```html
<h2 class="upzone-title">Carregue o PDF<span></span></h2>
<p class="upzone-sub">Saldo Atual da Exploração · Arraste ou clique</p>
```

Substituir por:
```html
<h2 class="upzone-title">Carregue os PDFs<span></span></h2>
<p class="upzone-sub">Saldo Atual da Exploração · Selecione um ou mais · Arraste ou clique</p>
```

- [ ] **Step 3: Substituir a função `lerPDF` (linhas ~1114-1124)**

Localizar:
```javascript
async function lerPDF(file){
  if(!file)return;
  showLoading('Lendo PDF no servidor Python...');
  const fd=new FormData();fd.append('pdf',file);
  try{
    const res=await fetch('/api/ler-pdf',{method:'POST',body:fd});
    const data=await res.json();
    if(data.erro){toast(data.erro,true);return}
    mostrarPDFPreview(data);
  }finally{hideLoading()}
}
```

Substituir por:
```javascript
async function lerPDF(files){
  if(!files||!files.length)return;
  const n=files.length;
  showLoading(n>1?`Lendo ${n} PDFs no servidor Python...`:'Lendo PDF no servidor Python...');
  const fd=new FormData();
  for(const f of files)fd.append('pdf',f);
  try{
    const res=await fetch('/api/ler-pdfs',{method:'POST',body:fd});
    const data=await res.json();
    if(data.erro){toast(data.erro,true);return}
    mostrarPDFPreview(data);
  }finally{hideLoading()}
}
```

- [ ] **Step 4: Atualizar o upzone reconstruído dentro de `mostrarPDFPreview` (linha ~1148)**

Localizar:
```javascript
document.getElementById('dz').innerHTML=`<span class="upzone-icon">✅</span><h2 class="upzone-title">PDF carregado: <span>${data.fazenda||'Exploração'}</span></h2><p class="upzone-sub">${fN(data.total)} animais · Clique para carregar outro</p><input type="file" id="pdf-inp" accept=".pdf" style="display:none" onchange="lerPDF(this.files[0])">`;
```

Substituir por:
```javascript
const _np=data.pdfs_processados||1;
document.getElementById('dz').innerHTML=`<span class="upzone-icon">✅</span><h2 class="upzone-title">${_np>1?_np+' PDFs carregados':'PDF carregado'}: <span>${data.fazenda||'Exploração'}</span></h2><p class="upzone-sub">${fN(data.total)} animais · Clique para carregar outros</p><input type="file" id="pdf-inp" accept=".pdf" multiple style="display:none" onchange="lerPDF(this.files)">`;
```

- [ ] **Step 5: Verificar no browser**

1. Abrir `http://localhost:5050`
2. Ir ao painel "Ler PDF"
3. Clicar na upzone — o seletor deve permitir seleção múltipla
4. Selecionar 2 PDFs → preview deve mostrar "2 PDFs carregados" e a soma dos animais

- [ ] **Step 6: Commit**

```bash
git add templates/index.html
git commit -m "feat: painel PDF suporta múltiplos arquivos"
```

---

### Task 3: Frontend — painel "Inserir Dados"

**Files:**
- Modify: `templates/index.html`
  - Linha ~391: input `insr-pdf-inp` — adicionar `multiple`, mudar onchange
  - Linhas ~1087-1102: função `lerPDFInserir`

- [ ] **Step 1: Atualizar o `<input>` do botão "Ler PDF" no painel Inserir Dados (linha ~391)**

Localizar:
```html
<input type="file" id="insr-pdf-inp" accept=".pdf" style="display:none" onchange="lerPDFInserir(this.files[0]);this.value=''">
```

Substituir por:
```html
<input type="file" id="insr-pdf-inp" accept=".pdf" multiple style="display:none" onchange="lerPDFInserir(this.files);this.value=''">
```

- [ ] **Step 2: Substituir a função `lerPDFInserir` (linhas ~1087-1102)**

Localizar:
```javascript
async function lerPDFInserir(file){
  if(!file)return;
  showLoading('Lendo PDF...');
  const fd=new FormData();fd.append('pdf',file);
  try{
    const res=await fetch('/api/ler-pdf',{method:'POST',body:fd});
    const data=await res.json();
    if(data.erro){toast(data.erro,true);return}
    FAIXAS.forEach((f,i)=>{document.getElementById(f.key+'_F').value=data.valores[i*2];document.getElementById(f.key+'_M').value=data.valores[i*2+1]});
    document.getElementById('f_faz').value=data.fazenda||'';
    document.getElementById('f_mun').value=data.municipio||'';
    document.getElementById('f_prop').value=data.proprietario||'';
    upd();
    toast('PDF carregado: '+(data.fazenda||'Exploração')+' ('+fN(data.total)+' animais)');
  }finally{hideLoading()}
}
```

Substituir por:
```javascript
async function lerPDFInserir(files){
  if(!files||!files.length)return;
  const n=files.length;
  showLoading(n>1?`Lendo ${n} PDFs...`:'Lendo PDF...');
  const fd=new FormData();
  for(const f of files)fd.append('pdf',f);
  try{
    const res=await fetch('/api/ler-pdfs',{method:'POST',body:fd});
    const data=await res.json();
    if(data.erro){toast(data.erro,true);return}
    FAIXAS.forEach((f,i)=>{document.getElementById(f.key+'_F').value=data.valores[i*2];document.getElementById(f.key+'_M').value=data.valores[i*2+1]});
    document.getElementById('f_faz').value=data.fazenda||'';
    document.getElementById('f_mun').value=data.municipio||'';
    document.getElementById('f_prop').value=data.proprietario||'';
    upd();
    const _np=data.pdfs_processados||1;
    toast((_np>1?_np+' PDFs carregados':'PDF carregado')+': '+(data.fazenda||'Exploração')+' ('+fN(data.total)+' animais)');
  }finally{hideLoading()}
}
```

- [ ] **Step 3: Verificar no browser**

1. Abrir `http://localhost:5050`
2. Ir ao painel "Inserir Dados"
3. Clicar em "📂 Ler PDF" — seletor deve permitir múltiplos arquivos
4. Selecionar 2 PDFs → campos preenchidos com soma, toast "2 PDFs carregados: ..."

- [ ] **Step 4: Commit**

```bash
git add templates/index.html
git commit -m "feat: inserir dados suporta múltiplos PDFs"
```
